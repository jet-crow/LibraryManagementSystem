import javax.swing.*;
import java.awt.*;

public class Client extends JFrame {
    public Client() {
        setTitle("图书管理系统");
        setSize(500, 500);
        setLayout(new GridLayout(4, 1));
        setLocationRelativeTo(null);
        setVisible(true);
    }
}
